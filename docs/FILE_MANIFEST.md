# File Manifest

The repository contains application code, deterministic domain logic, provider adapters, Supabase SQL, CI, deployment documentation, security/claims/change-control records, tests and release evidence. Run `npm run verify:repo` after any browser upload to verify the required handoff files remain present.
